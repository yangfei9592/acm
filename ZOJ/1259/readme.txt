核心思想

判断一个队列借助一个栈是否可以只通过出栈压栈，得到另一个队列状态。

如队列
12345678
pop push操作后的状态

54623187
需要判断每一个数，后面的比他小的所有数是不是从大到小排列。
比如5，后面比他小的数按顺序是4231。
要使5能出栈，则需要先让在5之前的数，按顺序入栈，才能使5出栈。而上例中4231不是按从大到小顺序的，所以该状态不可得到。

54632178
同理分析，该状态可以得到。
